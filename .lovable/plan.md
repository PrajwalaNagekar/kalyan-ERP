

# Replace Logo with Uploaded Malabar Gold & Diamonds Image

## Summary
Copy the user-uploaded logo to `src/assets/malabar-logo.png`, replacing the existing file. Both `Login.tsx` and `AppSidebar.tsx` already import from this path, so no code changes are needed — only the asset file swap.

## Changes

| File | Action |
|---|---|
| `src/assets/malabar-logo.png` | Replace with uploaded image (copy from `user-uploads://`) |

No code edits required. The new logo will automatically appear on the login page and sidebar dashboard.

